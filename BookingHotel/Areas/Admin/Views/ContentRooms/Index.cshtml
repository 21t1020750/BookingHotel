@model IEnumerable<BookingHotel.Models.Content_Room>

<h2>Phòng Hiển Thị</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Tạo Mới</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Tên Phòng</th>
            <th>Mô Tả</th>
            <th>Giá</th>
            <th>URL Hình Ảnh</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Price VNĐ/đêm</td>
                <td>@item.ImageUrl</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Sửa</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>