@model BookingHotel.Models.Content_Offer

<h2>Xóa Ưu Đãi Hiển Thị</h2>

<h3>Bạn có chắc muốn xóa?</h3>
<div>
    <h4>@Model.Title</h4>
    <dl class="row">
        <dt class="col-sm-2">Mô Tả</dt>
        <dd class="col-sm-10">@Model.Description</dd>
        <dt class="col-sm-2">URL Hình Ảnh</dt>
        <dd class="col-sm-10">@Model.ImageUrl</dd>
        <dt class="col-sm-2">Hết Hạn</dt>
        <dd class="col-sm-10">@Model.ValidUntil.ToString("dd/MM/yyyy")</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Xóa" class="btn btn-danger" /> |
        <a asp-action="Index">Quay Lại</a>
    </form>
</div>